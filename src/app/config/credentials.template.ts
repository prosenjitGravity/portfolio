/**
 * Local / Private Credentials Template
 * 
 * If you need to test with private API keys locally:
 * 1. Copy this file to `credentials.local.ts` in this folder.
 * 2. Populate your private keys or secrets.
 * 3. `credentials.local.ts` is explicitly ignored by .gitignore and will never be pushed to GitHub.
 */

export const LOCAL_SECRETS = {
  // Example: Private EmailJS / SendGrid API Key
  emailApiKey: '',
  emailServiceId: '',
  emailTemplateId: '',

  // Example: LinkedIn OAuth Client Secret (Keep on backend / proxy)
  linkedinClientSecret: '',

  // Example: Analytics Key
  googleAnalyticsId: ''
};
