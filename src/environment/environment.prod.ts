export const environment = {
  production: true,
  enableLogging: false,
  logLevel: 'ERROR',
  // Configure this to your same-origin API gateway in deployment.
  linkedinProfileApiUrl: '/api/linkedin/profile',
};
